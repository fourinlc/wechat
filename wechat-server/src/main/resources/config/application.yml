server:
  # 端口
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.77.218.220:3306/wechat_mmtls?useUnicode=true&useSSL=false&characterEncoding=utf8
    username: root
    password: 12345678
    hikari:
      pool-name: DateHikariCP
      minimum-idle: 5
      idle-timeout: 180000
      maximum-pool-size: 10
      auto-commit: true
      max-lifetime: 180000
      connection-timeout: 3000
      connection-test-query: SELECT 1

mybatis-plus:
  mapper-locations: classpath*:/mapper/*Mapper.xml
  type-aliases-package: com.xxx.server.pojo
  configuration:
    map-underscore-to-camel-case: false

wechat:
  rootUrl: http://120.77.218.220:18080
  file:
    basePath: D:/

logging:
  level:
    com.xxx.server.mapper: debug

jwt:
  # JWT存储的请求头
  tokenHeader: Authorization
  #JWT加解密使用的密钥
  secret: wechat-secret
  #JWT的超期限时间 (60*60*24)
  expiration: 604800
  #JWT负载中拿到开头
  tokenHead: Bearer